[project]
name = "rhoai-installer"
version = "0.1.0"
description = "RHOAI 3.5 SNO Ansible Installer"
requires-python = ">=3.12,<3.13"
dependencies = [
    "ansible-core>=2.17,<3.0",
    "ansible>=10.0",
    "kubernetes>=29.0",
    "jmespath>=1.0",
]

[tool.uv]
find-links = ["vendor/wheels"]
